# include<stdio.h>

int main(){
    int radius = 8;
    float pi = 3.14;
    printf("The area of circle is %f\n", pi * radius * radius);
    int height = 4;
    printf("The volume of cylinder is %f\n", pi * radius * height);
    return 0;
}